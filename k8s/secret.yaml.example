# Example secret configuration
# DO NOT commit actual secrets to version control
# Create the actual secret using:
# kubectl create secret generic toastmasters-secrets --from-literal=jwt-secret=YOUR_SECRET_HERE

apiVersion: v1
kind: Secret
metadata:
  name: toastmasters-secrets
type: Opaque
data:
  # Base64 encoded JWT secret
  # Generate with: echo -n "your-secret-key" | base64
  jwt-secret: eW91ci1zZWNyZXQta2V5
