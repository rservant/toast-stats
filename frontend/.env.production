# Production environment variables
# These will be embedded in the build at compile time

# API URL - served via GCP API Gateway (handles auth + CORS to Cloud Run)
VITE_API_BASE_URL=https://toast-stats-gw-9e9mo5c1.ue.gateway.dev/api
