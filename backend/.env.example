# Server Configuration
PORT=5001
NODE_ENV=development

# Toastmasters Dashboard URL
TOASTMASTERS_DASHBOARD_URL=https://dashboards.toastmasters.org

# Use mock data instead of real scraping (set to 'true' for development)
USE_MOCK_DATA=true

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=1h

# Cache Configuration
CACHE_DIR=./data/cache
CACHE_TTL=900

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS Configuration (production only)
# CORS_ORIGIN=https://yourdomain.com

# =============================================================================
# Storage Provider Configuration
# =============================================================================
# The application supports two storage backends:
# - 'local': Local filesystem storage (default, no GCP credentials required)
# - 'gcp': Google Cloud Platform storage (Firestore + Cloud Storage)

# Storage provider selection
# Valid values: 'local' | 'gcp'
# Default: 'local' (safe for development)
STORAGE_PROVIDER=local

# =============================================================================
# GCP Storage Configuration (required when STORAGE_PROVIDER=gcp)
# =============================================================================
# These variables are REQUIRED when STORAGE_PROVIDER is set to 'gcp'.
# The application will fail fast with a clear error if these are missing.

# GCP Project ID
# The Google Cloud project containing your Firestore database and GCS bucket
# Example: my-project-id
# GCP_PROJECT_ID=

# GCS Bucket Name
# The Cloud Storage bucket for raw CSV file caching
# Example: toast-stats-csv-cache
# GCS_BUCKET_NAME=

# Firestore Collection Name (optional)
# The Firestore collection for snapshot storage
# Default: 'snapshots'
# FIRESTORE_COLLECTION=snapshots

# =============================================================================
# GCP Emulator Configuration (for local development with GCP provider)
# =============================================================================
# When using STORAGE_PROVIDER=gcp for local development, you can use emulators
# instead of real GCP services. See docs/gcp-emulator-setup.md for details.

# Firestore Emulator Host
# Set this to use the Firestore emulator instead of real Firestore
# Example: localhost:8080
# FIRESTORE_EMULATOR_HOST=localhost:8080

# GCS Emulator Host
# Set this to use fake-gcs-server instead of real Cloud Storage
# Example: http://localhost:4443
# GCS_EMULATOR_HOST=http://localhost:4443
# STORAGE_EMULATOR_HOST=http://localhost:4443

# =============================================================================
# GCP Authentication (production)
# =============================================================================
# In GCP Cloud Run, authentication is automatic via the service account.
# For local development with real GCP services (not emulators), you need to
# authenticate using one of these methods:
#
# Option 1: Application Default Credentials (recommended for development)
#   Run: gcloud auth application-default login
#
# Option 2: Service Account Key File
#   Set GOOGLE_APPLICATION_CREDENTIALS to the path of your service account key
#   GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
#
# Option 3: Workload Identity (for GKE deployments)
#   Configure workload identity in your GKE cluster
#
# Required IAM Roles for the service account:
# - roles/datastore.user (for Firestore access)
# - roles/storage.objectAdmin (for GCS read/write access)
